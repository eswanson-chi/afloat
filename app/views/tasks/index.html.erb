<div class="page-header">
  <h1>
    Tasks
    <a href="/tasks/new" class="btn btn-lg btn-success">New Task</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>App</th>
        <th>App required</th>
        <th>Project group</th>
        <th>Activity</th>
        <th>Action</th>
        <th>Name</th>
        <th>Description</th>
        <th>Actions</th>
      </tr>

      <% @tasks.each do |task| %>
      <tr>
        <td><%= task.app_id %></td>
        <td><%= task.app_required %></td>
        <td><%= task.project_group_id %></td>
        <td><%= task.activity_id %></td>
        <td><%= task.action_id %></td>
        <td><%= task.name %></td>
        <td><%= task.description %></td>
        <td>
          <a href="/tasks/<%= task.id %>" class="btn btn-primary">Show</a>
          <a href="/tasks/<%= task.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_task/<%= task.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
